<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
    android:name="MainActivity" — no leading dot, no package. Android's
    manifest rule: a leading "." means "prepend the namespace package"; a
    name with no leading dot is used as-is as the fully qualified class
    name. MainActivity.kt has no package declaration (Kotlin's default
    package), so its fully qualified name genuinely is just "MainActivity".
    This is why it isn't written as ".MainActivity" the way a namespaced
    Activity normally would be.
    -->
    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.App">

        <activity
            android:name="MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
